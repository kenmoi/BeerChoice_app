<div class="container">
  <div class="row mx-auto">
    <div class="main-title col-md-11">
      <h2 class="page-title">
        <span class="beer">ユーザーページ</span>
      </h2>
      <div class="profileBox">
        <div class="profile">
          <div class="content">
            <div class="d-flex">
              <% if @user.profile_image_id.present? %>
               <p class="col-md-4"><%= attachment_image_tag @user, :profile_image, :fill, 150, 150, class: 'picture' %></p>
              <% end %>
              <div class="col-md-6 mt-3"><strong class="text-danger"><%= @user.name %></strong>さん (<%= @user.prefecture_id %> <%= @user.sex %>)<br>
               <p class="mt-2"><%= link_to "投稿する" %></p>
              </div>
            </div>
            <% if @user.introduction.present? %>
              <div class="info col-md-11 mx-auto">
               <p><%= simple_format(@user.introduction) %></p>
              </div>
            <% end %>
          </div>
        </div>
        <h2 class="beer-style box-item">好きなビアスタイル</h2>
        <div class="col-md-11">
          <p><%= @user.favorite_style %></p>
        </div>
        <h2 class="favorite-beer box-item">お気に入りのビール</h2>
        <div class="col-md-11">
          <p><%= simple_format(@user.favorite_beer) %></p>
        </div>
        <% if current_user == @user %>
          <p class="user-show">
            <%= link_to "登録情報・プロフィールの変更>>", user_path(@user), class: 'text-info' %>
            <br>※登録情報とプロフィールの編集はこちらより行えます。
          </p>
        <% end %>
      </div>
    </div>
  </div>
</div>